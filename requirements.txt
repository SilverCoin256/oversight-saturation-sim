# Exact versions used to produce every figure and JSON in this release
# (Python 3.14.6). The looser lower bounds below numpy>=1.24, scipy>=1.10,
# matplotlib>=3.7, requests>=2.28 also reproduce the synthetic results; the
# pins are the audited configuration.
numpy==2.4.4
scipy==1.17.1
matplotlib==3.10.9
requests==2.32.3
